// Экспонат — самодостаточный фрагмент интерфейса проекта.
// У него собственное фиксированное оформление (дизайн того проекта),
// не зависящее от текущего дизайн-режима портфолио.
import type { Locale } from "@/content/site";

export type ExhibitProps = {
  locale: Locale;
};
